class ExpenseItem {
    final String name;
    final String amount;
    final DateTime dateTime;
    
    //creating constructor
    ExpenseItem({
        required this.name,
        required this.amount,
        required this.dateTime
    });
}